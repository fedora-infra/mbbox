apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ pvc_name }}
  labels:
    app: koji-hub
spec:
  accessModes:
    - {{ pvc_mode }}
  resources:
    requests:
      storage: {{ pvc_size }}
